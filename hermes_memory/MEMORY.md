Default project folder: /opt/hermes/work/ — use this directory for all project work unless the user specifies otherwise.
§
Email account: hermesagent471@gmail.com — password stored in ~/.hermes/secrets/gmail_app_password (created by user for Hermes Agent access).
§
Never push directly to main/master. Always push to a feature branch so the user can review first. GitHub PAT stored at /root/.git-credentials (classic PAT for hermesagent471-rgb, credential.helper=store).
